# Система управления банковскими картами

REST API для управления банковскими картами и транзакциями с ролевой моделью доступа и JWT-аутентификацией.

## 📦 Технологический стек
- **Язык**: Java 17
- **Фреймворк**: Spring Boot 3.4.6
- **База данных**: PostgreSQL 14
- **Безопасность**: JWT аутентификация
- **Инструменты**:
    - Spring Data JPA
    - Spring Security
    - SpringDoc OpenAPI
    - Liquibase
    - Lombok
    - Maven

## ⚙️ Функционал

### 👤 Управление пользователями:

- **Публичные операции**:
  - Регистрация новых пользователей с валидацией данных
  - Аутентификация с использованием JWT-токенов

- **Операции пользователя**:
  - Обновление собственного профиля (имя, email, пароль)

- **Административные операции**:
  - Управление ролями пользователей (добавление/удаление ролей)
  - Просмотр списка всех пользователей с пагинацией и сортировкой
  - Получение детальной информации о пользователе по ID
  - Удаление пользователей

### 💳 Управление банковскими картами:

- **Операции пользователя**:
  - Создание новых карт
  - Удаление собственных карт
  - Просмотр списка своих карт с пагинацией и сортировкой

- **Административные операции**:
  - Просмотр всех карт с фильтрацией по статусу и владельцу
  - Модерация статуса карт (активация, блокировка)
  - Удаление любых карт из системы

### 💸 Транзакции:

- **Операции пользователя**:
  - Переводы между картами
  - Просмотр истории операций по конкретной карте с фильтрацией по статусу

- **Административные операции**:
  - Изменение статуса транзакций
  - Просмотр всех транзакций с фильтрацией, пагинацией и сортировкой

### 🔒 Безопасность:

- Шифрование номеров карт в базе данных
- Маскирование номеров карт при отображении
- Ролевая модель доступа (USER, ADMIN)
- Валидация всех входящих данных
- Защита от основных уязвимостей веб-приложений

## 🏗️ Структура проекта

```
├── config/               # Конфигурация Spring и безопасности
├── controller/           # REST контроллеры
│   ├── card/             # Контроллеры для управления картами
│   ├── role/             # Контроллеры для управления ролями
│   └── user/             # Контроллеры для управления пользователями
├── dto/                  # Data Transfer Objects
├── entity/               # Модели данных
├── exception/            # Обработка исключений
├── mapper/               # Конвертеры между DTO и Entity
├── repository/           # Репозитории для доступа к данным
├── security/             # Компоненты безопасности
└── service/              # Бизнес-логика
```

## 👥 Роли пользователей

### USER
- Базовая роль, присваивается при регистрации
- Управление собственным профилем
- Создание и управление своими картами
- Выполнение транзакций
- Просмотр истории своих операций

### ADMIN
- Расширенная роль для администрирования системы
- Управление всеми пользователями
- Управление ролями пользователей
- Модерация карт (блокировка/разблокировка)
- Просмотр и управление всеми транзакциями

## 🚀 Запуск проекта

### Вариант 1: Запуск с использованием Docker

1. Клонируем репозиторий
```bash
git clone https://github.com/vsvldm/Bank_REST.git
cd Bank_REST
```

2. Собираем проект
```bash
mvn clean package
```

3. Запускаем с помощью Docker Compose
```bash
docker-compose up -d
```

### Вариант 2: Локальный запуск

1. Клонируем репозиторий
```bash
git clone https://github.com/vsvldm/Bank_REST.git
cd Bank_REST
```

2. Настраиваем подключение к базе данных в `application.properties` или через переменные окружения

3. Запускаем приложение
```bash
mvn spring-boot:run
```

## 🌐 Документация API

После запуска приложения документация API доступна через:

- **Swagger UI**: http://localhost:8080/swagger-ui/index.html
- **OpenAPI спецификация**: http://localhost:8080/v3/api-docs

В документации подробно описаны все доступные эндпоинты, параметры запросов и ответов, а также схемы данных.

## 🧪 Тестирование

Проект включает модульные и интеграционные тесты:

```bash
# Запуск всех тестов
mvn test

# Запуск только модульных тестов
mvn test -Dtest=*Test

# Запуск только интеграционных тестов
mvn test -Dtest=*IT
```

## 📋 Требования к системе

- Java 17 или выше
- Maven 3.6+
- Docker и Docker Compose (для запуска в контейнерах)
- PostgreSQL 14 (при локальном запуске без Docker)